# Exclude development-only paths from release archives generated by `git archive`
# (GitHub "Source code" downloads on a release honour these rules). The files
# remain in the repository so the rendered README still displays the schematic.
assets/        export-ignore
.gitattributes export-ignore
.gitignore     export-ignore
